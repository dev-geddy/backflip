/**
 * /backflip — admin dashboard (authenticated).
 * Placeholder: real dashboard to be implemented in a later phase.
 */
export default function BackflipDashboardPage() {
  return (
    <main className="flex min-h-svh items-center justify-center p-6">
      <div className="text-sm text-muted-foreground">
        Admin dashboard — placeholder (setup only).
      </div>
    </main>
  )
}
