/**
 * /backflip/login — admin login (public, unauthenticated).
 * Placeholder: Google-auth login form to be implemented in a later phase.
 */
export default function LoginPage() {
  return (
    <main className="flex min-h-svh items-center justify-center p-6">
      <div className="text-sm text-muted-foreground">
        Admin login — placeholder (setup only).
      </div>
    </main>
  )
}
